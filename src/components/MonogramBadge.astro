---
import { siteConfig } from "../config/site";

interface Props {
  label?: string;
  class?: string;
}

const { label = "Arkham Advisory", class: className = "" } = Astro.props;
---

<div class={`monogram-badge ${className}`.trim()} aria-label={label}>
  <img
    src={siteConfig.assets.logos.monogramDark}
    alt=""
    width="1254"
    height="1254"
    loading="lazy"
    decoding="async"
  />
</div>

<style>
  .monogram-badge {
    display: inline-grid;
    width: clamp(4rem, 11vw, 7rem);
    aspect-ratio: 1;
    place-items: center;
    border: 1px solid var(--border-subtle);
    border-radius: 50%;
    background: var(--bg-body);
  }

  .monogram-badge img {
    width: 72%;
    height: auto;
  }
</style>
